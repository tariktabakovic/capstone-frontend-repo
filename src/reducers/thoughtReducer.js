import uuid from 'uuid';

const initialState= {
    thoughts: [
        {id: uuid(), name: 'Be nice'},
        {id: uuid(), name: 'Be fun'},
        {id: uuid(), name: 'Be now'},
        {id: uuid(), name: 'Be happy'}
    ]
}